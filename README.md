
  # JetDash

  This is a code bundle for JetDash. The original project is available at [(bit.ly/4h9EaGH)](https://bit.ly/4h9EaGH)
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
